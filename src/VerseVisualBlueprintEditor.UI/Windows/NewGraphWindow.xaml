<Window x:Class="VerseVisualBlueprintEditor.UI.Windows.NewGraphWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Create New Blueprint" Height="200" Width="400"
        WindowStartupLocation="CenterOwner"
        Style="{StaticResource UnrealWindow}"
        ResizeMode="NoResize"
        ShowInTaskbar="False">
    <Grid Background="#1a1a1a" Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Blueprint Name:" Foreground="#b0b0b0" Margin="0,0,0,8"/>
        
        <TextBox Grid.Row="1" 
                 x:Name="NameTextBox"
                 Style="{StaticResource UnrealTextBox}"
                 Text="MyDevice"
                 Foreground="#ffffff"
                 Margin="0,0,0,16"/>

        <TextBlock Grid.Row="2" Text="Class Name:" Foreground="#b0b0b0" Margin="0,0,0,8"/>
        
        <TextBox Grid.Row="3" 
                 x:Name="ClassNameTextBox"
                 Style="{StaticResource UnrealTextBox}"
                 Text="my_device"
                 Foreground="#ffffff"
                 Margin="0,0,0,16"/>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Create" 
                    Style="{StaticResource UnrealButton}"
                    Margin="0,0,4,0"
                    Width="80"
                    Click="Create_Click"/>
            <Button Content="Cancel" 
                    Style="{StaticResource UnrealButton}"
                    Width="80"
                    Click="Cancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
